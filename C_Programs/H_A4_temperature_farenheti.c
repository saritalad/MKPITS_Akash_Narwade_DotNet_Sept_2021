/* 100 degree celcius into farenheti scale*/
#include<stdio.h>
int main()
{
float celcius=100,f;
printf("enter the temperature in celcius");
scanf("%f",&celcius);
f=(celcius*1.8)+32;
printf("Temperature into Farenheti scale=%0.1f\n",f);

  return 0;
  }

