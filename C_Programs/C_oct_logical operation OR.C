/* logical operation */
/* pre processor directive */
#include<stdio.h>

/* logical operation */
int main()
{
int a,b,c;
 
 /* find greatest number*/
printf("\n enter number");
scanf("%d",&a);
if(((a%2==0)|| (a>0)) && (a>2))
printf("\n %d is even or positive number ",a);
else
printf("\n %d is not even or positive number ",a);

return 0;

}

